# Fresh Rebuild GitHub Staging

The `fresh-rebuild/` tree is the staged Shadow Shinobi rebuild and is intentionally separate from the legacy application currently at repository root.

The complete binary asset library is not committed here because the connected GitHub API cannot upload binary repository files. Use the supplied local asset archive with this source tree until a new empty GitHub repository is created and the asset library can be pushed with normal Git/LFS tooling if desired.
